// Put your configuration in a file named config.js and it will be included in the app.
// If you are not running the API on the same server, you will need to provide configuration details about where to find it.

var SocialHarvestConfig = {
	"apiHost": "http://localhost:3000"
};